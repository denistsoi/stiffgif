// import {ipcRenderer} from 'electron';
// const ipc = ipcRenderer;

// export default (app, query) =>{
//   // var offset = app.giphy.length;
//   console.log(app, this);
//   app.loading = true;
//   ipc.send('search:giphy', { scope: 'search', query: query, offset: offset });
// }